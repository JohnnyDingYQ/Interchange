public enum Side { Start, End, Both }
public enum Direction { In, Out, Both }
public enum Orientation { Left, Right }
public enum BuildMode { Ghost, Actual }
public enum RoadRemovalOption { Default, Divide, Combine }